# 外部APIキーは基本的に不要（すべてローカル処理）
DATABASE_URL="file:./storage/db.sqlite"

# 任意（Geminiをハイブリッド判定に使う場合のみ、無料枠内での利用）
GEMINI_API_KEY=

# python3 の実行コマンド名（環境によって python3 / python）
PYTHON_BIN=python3

# faster-whisper のモデルサイズ（large-v3 推奨、CPUが遅い場合は medium も選択可）
WHISPER_MODEL=large-v3
